# hdf5_pipeline 使用文档

机器人数据流水线工具：**HDF5 数据重命名 → 异常帧检测 → MP4 渲染 → 视频打标归档**。
基于 Streamlit 提供图形界面，同时提供命令行入口，支持 HDF5 与 LeRobot Parquet 两种数据格式。

---

## 一、环境准备

### 1.1 创建 Python 环境（二选一）

**方式 A：conda 环境（推荐，自带 ffmpeg）**

```bash
conda env create -f environment.yml
conda activate hdf5_pipeline
```

**方式 B：项目内虚拟环境**

```bash
conda create -p ./conda_env python=3.11 -y
conda activate ./conda_env
pip install -r requirements.txt
```

### 1.2 安装本项目（供 CLI 命令使用）

```bash
pip install -e .
```

安装后即可使用 `hdf5-pipeline` 命令行入口。

### 1.3 验证环境

```bash
./conda_env/bin/python -m pytest -q    # 应显示全部测试通过
```

---

## 二、目录结构

```
hdf5_pipeline/
├── hdf5_pipeline/            # 主包
│   ├── __main__.py / cli.py  # 命令行入口
│   ├── core/                 # 核心工具
│   │   ├── config.py         #   配置加载/保存
│   │   ├── constants.py      #   常量（关节名、严格度预设等）
│   │   ├── hdf5_utils.py     #   HDF5/Parquet 读写、自然排序、帧数统计
│   │   ├── validator.py      #   数据文件结构校验
│   │   ├── utils.py          #   跨平台文件夹选择器
│   │   └── video_utils.py    #   视频信息/取帧
│   ├── label/                # 打标系统
│   │   ├── app.py            #   Streamlit 主界面（6 个标签页）
│   │   ├── database.py       #   SQLite 数据库操作
│   │   └── state.py          #   session_state 管理
│   ├── quality/              # 质量检测（异常帧检测）
│   │   ├── detector.py       #   检测算法（DeltaAction → 分位数打分）
│   │   ├── checker.py        #   检测流程封装
│   │   └── *_checker.py      #   HDF5 / LeRobot 封装
│   ├── render/               # 视频渲染引擎（HDF5 → MP4）
│   ├── rename/               # 数据重命名
│   ├── ui/                   # Streamlit 页面组件
│   │   ├── module_app.py     #   独立模块页入口
│   │   ├── quality_tab.py    #   质量检测标签页
│   │   ├── render_tab.py     #   视频渲染标签页
│   │   ├── rename_tab.py     #   重命名标签页
│   │   └── common.py         #   公共常量/回调
│   └── parquet_ui_redo/      # Spirit parquet 专用（转换/质检/校验/UI）
├── config.json               # 运行配置（路径 + 自定义属性）
├── environment.yml           # conda 环境定义
├── requirements.txt          # 锁定版本依赖
├── pyproject.toml            # 项目元信息与打包
├── tests/                    # pytest 测试
└── test_data/                # 本地示例数据（数据库/原始数据/归档目录）
```

---

## 三、配置文件 config.json

`config.json` 决定所有路径与打标属性，可在界面「⚙️ 配置」页或直接编辑文件修改。

### 3.1 `paths` 路径配置

| 键 | 含义 |
|---|---|
| `db_dir` | SQLite 数据库文件路径（不存在时自动创建 `label.db`） |
| `raw_dir` | 原始 HDF5 数据目录 |
| `mp4_dir` | MP4 视频目录 |
| `good_dir` | 标记为 good 的 HDF5 归档目录 |
| `bad_dir` | 标记为 bad 的 HDF5 归档目录 |
| `csv_dir` | 质量检测报告导出目录（tab4 异常处理自动读取其中的 `outlier_frames.csv`） |

### 3.2 `custom_cols` 自定义属性

打标时需要记录的自定义标签（如天气、背景），每个属性包含：

```json
"attr_weather": {
    "label": "天气",          // 界面显示名
    "option": ["下雨天", "晴天", "雾霾天"],   // 可选值
    "type": "text"            // 输入类型
}
```

---

## 四、启动方式

### 4.1 图形界面（推荐）

**完整打标系统**（6 个标签页：重命名 / 质量检测 / 视频渲染 / 视频打标 / 数据总览 / 配置）：

```bash
streamlit run hdf5_pipeline/label/app.py
# 或
hdf5-pipeline label
```

**独立模块页**（只启动其中一个模块）：

```bash
hdf5-pipeline ui --module quality     # rename / quality / render
```

**Spirit parquet 独立界面**：

```bash
hdf5-pipeline spirit ui
```

### 4.2 命令行

| 命令 | 功能 |
|---|---|
| `hdf5-pipeline rename <src> <out>` | 递归收集 src 下所有数据文件，按自然排序重命名并输出到 out |
| `hdf5-pipeline check <dir> [--strictness] [--format]` | 对目录下所有文件做异常帧检测，输出报告（strictness: loose/medium/strict，format: hdf5/lerobot） |
| `hdf5-pipeline pipeline <src> <out> <csv_json_out> <mp4_out>` | 一键执行：重命名 → 质量检测 → MP4 渲染 |
| `hdf5-pipeline render` | 启动独立视频渲染页面 |
| `hdf5-pipeline label` | 启动完整打标系统 |
| `hdf5-pipeline spirit convert <raw_dir> <out_dir> [--copy-videos]` | Spirit 原始 parquet → 标准 LeRobot v2.1 数据集（默认视频软链接） |
| `hdf5-pipeline spirit check <raw_dir> <out_csv> <out_json> [--strictness]` | Spirit 原始数据异常帧检测 |
| `hdf5-pipeline spirit validate <raw_dir>` | Spirit 结构完整性 + event_log 一致性校验 |
| `hdf5-pipeline spirit ui` | Spirit 独立 Streamlit 界面 |

---

## 五、功能模块（图形界面）

### 5.1 侧边栏：工作路径配置

- 配置 5 个路径：数据库、HDF5 原始目录、MP4 目录、good/bad 归档目录，每个路径可手动输入或「浏览」选择。
- 「扫描文件夹并同步数据库」：扫描 `mp4_dir` 与 `raw_dir`，将配对的 MP4+HDF5 注册进数据库（只新增不覆盖已打标记录）。
- 若数据库路径无效（文件缺失或尚未包含 label 表），界面会弹出「数据库不可用」提示，按指引重新设置即可。

### 5.2 Tab1 文件重命名

递归获取目录下所有数据文件，按自然排序（保证 `episode_2` 在 `episode_10` 前）重命名并放置到统一目录。

### 5.3 Tab2 质量检测

- **路径设置**：数据目录、CSV 导出目录、JSON 导出目录。
- **检查设置**：严格度（loose/medium/strict）、数据格式（HDF5 / LeRobot Parquet）。
- 执行检测后展示统计：扫描文件数、异常帧数、总帧数、异常最多的维度与文件。
- 导出 `outlier_frames.csv`（异常帧明细）与 `outlier_summary.json`（统计摘要）到指定目录。

### 5.4 Tab3 视频渲染

- **路径设置**：HDF5 数据目录、MP4 导出目录。
- **渲染选项**：是否显示顶端图像、是否显示动作曲线、16 维动作曲线逐维开关、左右臂 7 维关节曲线逐关节开关。
- **输出选项**：并发数（建议不超过 CPU 核数）、跳过已生成视频（断点续传）。
- 「开始转换」后台多进程渲染，「终止转换」安全中断；渲染日志面板每 2 秒自动刷新进度。

### 5.5 Tab4 视频打标（核心）

**① 异常文件处理（CSV 自动导入）**

自动读取 `csv_dir/outlier_frames.csv`（质量检测导出），列出其中的异常文件，支持：
- 「扫描并标红」：异常文件在文件选择框带 🔴 前缀、下方红字列表提醒；
- 「全部标记 bad」：批量把异常文件标记为 bad 并移动到 `bad_dir`；
- 「取消标红」：清空标红（已标记 bad 的文件不撤销）。

**② 文件列表**

- 「获取并更新记录列表」从数据库刷新记录。
- 下拉框选择文件（显示质量与文件名），「上一条 / 下一条」切换。
- 顶部进度条显示已完成打标比例。

**③ 质量分类**

- 四个按钮：good ✅ / bad ❌ / pending ❎ / unlabeled ☑️。
- 标记 good 时文件移动到 `good_dir`，bad 移动到 `bad_dir`，pending/unlabeled 保留在 `raw_dir`；数据库同步更新质量与路径。

**④ 属性设置**

- 每个自定义属性一个下拉框，选择后点「确认」写入记录。

**⑤ 预览区**

- 右侧显示所选视频的首帧、末帧缩略图与完整视频。

### 5.6 Tab5 数据总览

- **自定义查询**：支持 SQL 风格 WHERE 条件，属性用 `@` 引用（如 `@天气 = "晴天"`、`@背景 LIKE "%室%"`），支持 `AND`/`NOT` 等逻辑组合；可多选返回列。「清除所有筛选」一键清空条件并回到全量第一页。
- **批量修改**：按当前查询条件批量改质量（good/bad/pending/unlabeled），或批量修改某属性。
- **数据列表**：分页展示（每页 10 条），每条可点开查看视频/首末帧、直接改质量、改属性。底部提供上一页 / 下一页、页码信息与「跳转页」输入框，可直接输入目标页码跳转。

### 5.7 Tab6 配置

- **新建属性**：输入键名（英文）、显示名称、输入类型、选项（逗号分隔）。
- **已保存属性**：支持编辑、删除。
- **导入/导出**：下载属性配置 JSON 备份，或上传 JSON 恢复。

---

## 六、数据流与归档约定

```
HDF5 原始数据
   │  rename（Tab1 / CLI）→ 自然排序重命名
   ▼
质量检测（Tab2 / CLI check）
   │  导出 outlier_frames.csv → csv_dir
   ▼
Tab4 异常处理：标红提醒 / 批量标记 bad
   │
   ▼
打标归档（Tab4 / Tab5 批量）:
   good → good_dir     bad → bad_dir     pending/unlabeled → raw_dir
   │
   ▼
SQLite（label 表）记录：hdf5/mp4 路径、质量、属性、时间

视频渲染（Tab3 / CLI render）: HDF5 → MP4，独立于打标流程
```

数据库 `label` 表为唯一打标事实来源，所有打标/归档操作都会写库；界面通过版本号自动刷新缓存，多页面之间数据保持一致。

---

## 七、测试

```bash
./conda_env/bin/python -m pytest -q
```

测试覆盖：数据库操作、HDF5/Parquet 读写、质量检测算法、渲染引擎、重命名与数据流水线。

---

## 八、常见问题

**1. 目录重命名后 conda 环境失效？**

本项目环境为 `./conda_env`（相对路径创建）。若整个项目目录移动，`conda activate` 需用新绝对路径：

```bash
conda activate /Users/karis/projects/hdf5_pipeline/conda_env
```

直接运行 `./conda_env/bin/python` 不受影响。同理，`config.json` 中的绝对路径需同步更新。

**2. 数据库在哪里？误删会怎样？**

默认在 `config.json` 的 `db_dir`（本项目为 `test_data/db/label.db`）。删除后重新在侧边栏选择数据库路径或重新扫描配对即可重建。

**3. 换 IDE / 目录移动后打不开？**

- VSCode：用新路径重新「打开文件夹」即可。
- IntelliJ IDEA：`.idea/` 中模块名仍为旧名，建议重新导入项目。

**4. 为什么某个文件在打标界面标红但仍可标记？**

标红只是提醒（来自质量检测报告），不影响正常打标操作。标记 good/bad 后会自动从标红列表移除。

---