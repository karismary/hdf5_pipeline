## 一、架构分层

```
┌─ 表现层  ui/ + label/app.py（Streamlit 页面）
├─ 业务层  label/database.py（打标/归档）、quality/、render/、rename/、parquet_ui_redo/
└─ 核心层  core/（配置、数据读写、校验、常量）
```

**依赖方向严格自上而下**：UI → 业务层 → 核心层。`core/` 不得 import `ui/` 或业务层；业务层不得 import `ui/`。新代码请保持这一方向。

---

## 二、核心层接口（hdf5_pipeline/core/）

### 2.1 config.py — 配置管理

| 函数 | 签名 | 说明 |
|---|---|---|
| `load_config` | `(config_path: str \| Path \| None = None) -> dict` | 加载配置，文件不存在返回默认值 |
| `save_config` | `(config_data: dict, config_path=None) -> None` | 写回 JSON（UTF-8、缩进 4） |
| `get_default_config` | `() -> dict` | 返回默认配置副本，避免污染全局常量 |

```python
from hdf5_pipeline.core.config import load_config, save_config
config = load_config("config.json")
config["paths"]["raw_dir"] = "/new/path"
save_config(config)
```

### 2.2 hdf5_utils.py — 数据读写

| 函数 | 签名 | 说明 |
|---|---|---|
| `get_sorted_files` | `(folder: str, file_type: list, return_type: int) -> list[Path] \| list[str]` | 递归扫描，自然排序。`return_type=0` 返回 Path，`=1` 返回文件名 str |
| `get_hdf5_files` | `(folder: str, format="hdf5") -> list[Path]` | 按格式（hdf5/lerobot）递归扫描 |
| `get_hdf5_frame_count` | `(h5_file: str, format="hdf5") -> int \| None` | 帧数，兼容 HDF5/Parquet |
| `load_images_from_hdf5` | `(path: str) -> (dict[str, np.ndarray], int)` | 各相机图像 `{cam: (T,H,W,3) uint8}` + 帧率 |
| `load_actions_from_hdf5` | `(path: str, n_frames: int) -> np.ndarray` | 自动识别 `action`/`actions` key |
| `load_joints_from_hdf5` | `(path: str, n_frames: int) -> (left, right)` | 左右臂关节，缺失为 None |
| `load_raw_30dim` | `(path: str) -> (action_30, state_30)` | 原始 30 维数据 |
| `project_30_to_16` | `(x30: np.ndarray) -> np.ndarray` | 30 维 → 训练 16 维 |
| `normalize_image_array` | `(arr: np.ndarray) -> np.ndarray` | NCHW→NHWC、float→uint8、灰度→RGB |
| `natural_sort_key` | `(name: str) -> list` | 自然排序 key，保证 `episode_2` 在 `episode_10` 前 |

```python
from hdf5_pipeline.core.hdf5_utils import get_sorted_files, load_raw_30dim, project_30_to_16
files = get_sorted_files("./raw", [".hdf5"], 1)          # 返回文件名列表
action_30, state_30 = load_raw_30dim(files[0])
action_16 = project_30_to_16(action_30)
```

### 2.3 其他核心模块

| 模块 | 函数 | 说明 |
|---|---|---|
| validator.py | `validate_file(file_path) -> (ok, errors)` | HDF5/Parquet 结构校验 |
| utils.py | `pick_folder() -> str` | 跨平台文件夹选择弹窗，取消返回 `""` |
| video_utils.py | `get_video_info(mp4_path) -> (frames, fps)` | 视频帧数/帧率 |
| video_utils.py | `get_frame(mp4_path, n) -> np.ndarray \| None` | 读指定帧（RGB），失败 None |
| video_utils.py | `extract_first_last_frames(mp4_path) -> (first, last)` | 首尾帧，失败位置为 None |
| constants.py | `JOINT_NAMES` / `STRICTNESS_PRESETS` / `DEFAULT_DELTA_MASK_16` / `DIM_*` | 关节名、严格度预设、掩码、维度索引 |

---

## 三、数据库层（hdf5_pipeline/label/database.py）

连接工厂统一 `_connect(db_path)`，设置 `row_factory=sqlite3.Row`，**查询结果支持按列名取值**：`row["mp4_name"]`。

| 函数 | 签名 | 说明 |
|---|---|---|
| `init_db` | `(db_path: str) -> None` | 建 `label` 表 + 索引，不覆盖已有数据 |
| `scan_pairs` | `(db_path, mp4_dir, raw_dir) -> int` | 扫描配对 MP4+HDF5 注册入库，返回新增对数 |
| `add_label` | `(db_path, mp4_name, hdf5_path=None, quality=None, attr=None) -> None` | 更新单条记录，只更新传入字段 |
| `add_labels` | `(db_path, records, quality=None, if_qualify=False, target_dir=None, attr=None) -> None` | 批量更新，单事务。`if_qualify=True` 时同时更新 hdf5_path 到 target_dir |
| `update_attrs` | `(db_path, records, new_attr) -> None` | 批量改属性，`new_attr=(config_key, option)` |
| `query_records` | `(db_path, columns, where_clause, limit=None, offset=0) -> (records, err)` | 自定义查询，返回 **`(结果, 错误)` 元组**，err 为 None 表示成功 |
| `get_list` | `(db_path) -> list[Row]` | 全表记录 |
| `get_records` | `(db_path, mp4_name) -> Row \| None` | 单条记录 |
| `count_list` | `(db_path, where_clause=None) -> int` | 计数，DB 不存在返回 0 |
| `translate_where` | `(condition: str, attr_map: dict) -> str` | 把 `@属性` 引用翻译成 SQL `json_extract` |

```python
records, err = query_records(db, "*", 'quality = "good"', limit=10, offset=0)
if err:
    print("查询失败:", err)
else:
    for r in records:
        print(r["mp4_name"], r["quality"])
```

> `label` 表字段：`id, hdf5_name, hdf5_path, mp4_name, mp4_path, quality, attr(JSON), created_at, labeled_at`。

---

## 四、界面状态层（hdf5_pipeline/label/state.py）

统一封装 `session_state` 读写，**页面代码不直接操作 `st.session_state` 之外的裸键**。

```python
from hdf5_pipeline.label.state import get, set, pop, init_state, bump_db_version, S_RECORDS

init_state()                       # 应用启动时调用一次，初始化所有默认值
set(S_RECORDS, get_list(db))       # 写
records = get(S_RECORDS)           # 读
bump_db_version()                  # 任何写库操作后调用，触发各页缓存刷新
```

**注意：`state.set` 遮蔽了 Python 内置 `set()`。** 在打标相关代码里需要集合时，一律用 `frozenset()`（如 `get(S_MARKED_RED, frozenset()) | {name}`）。

常用常量：`S_RECORDS`（tab4 记录缓存）、`S_SELECTED`（当前选中记录）、`S_MARKED_RED`（标红集合）、`S_DB_VERSION`（跨页版本号）、`S_TOAST`（一次性提示消息）。

---

## 五、打标业务逻辑（hdf5_pipeline/label/app.py）

| 函数 | 签名 | 说明 |
|---|---|---|
| `qualify_and_move` | `(db_path, mp4_name=None, raw_path=None, target_dir=None, quality=None, compare_quality=None, if_quantity=False, quantity_records=None) -> list[tuple] \| None` | 移动文件到归档目录 + 更新 DB。单条/批量两种模式，失败回滚 |
| `quality_module` | `(key_name, if_session_state=True, session_state="selected", target=None) -> None` | 渲染 good/bad/pending/unlabeled 四按钮 |
| `attrs_module` | `(template_attrs, key_name, if_session_state=True, session_state="selected", target=None) -> None` | 渲染属性下拉 + 确认 |

```python
# 单条：把当前文件标 bad 并归档到 bad_dir
qualify_and_move(db, mp4_name=r["mp4_name"], raw_path=r["hdf5_path"],
                 target_dir=config["paths"]["bad_dir"], quality="bad",
                 compare_quality=r["quality"])

# 批量：一次性把一批记录标 good 归档
qualify_and_move(db, target_dir=config["paths"]["good_dir"], quality="good",
                 if_quantity=True, quantity_records=records)
```

---

## 六、质量检测（hdf5_pipeline/quality/）

### 6.1 检测算法（detector.py）

| 函数 | 签名 | 说明 |
|---|---|---|
| `compute_outliers` | `(episodes, mask, strictness="strict", min_score=None, top_k_per_episode=None, top_k_global=None, min_denom=None) -> (rows, summary)` | 核心算法：DeltaAction → 分位数打分 → 排序截断 |
| `parse_mask` | `(mask_str: str) -> np.ndarray` | `"1,1,0,..."` → 布尔数组 |
| `export_results` | `(outlier_rows, summary, out_csv, out_json) -> bool` | 写 CSV/JSON；无异常提前返回 True |
| `apply_delta` / `fit_quantiles` | — | 计算 Delta / 拟合分位数 |

`episodes` 为 `[(path, action, state, frame_index), ...]`，其中 action/state 为 (T, D)，frame_index 为 (T,) 帧号数组。

### 6.2 检测入口（checker.py）

```python
run_quality_check(
    data_dir, format="hdf5",            # format: "hdf5" | "lerobot"
    out_csv="outlier_frames.csv", out_json="outlier_summary.json",
    strictness="strict",                # "loose" / "medium" / "strict"
    # 可选手动覆盖预设：min_score / top_k_per_episode / top_k_global / min_denom
    loader=None,                        # 自定义加载函数 (data_dir) -> episodes，优先级最高
) -> dict   # 统计摘要
```

`load_episodes(data_dir, format, *, action_key, state_key, frame_key)` 批量加载文件；`hdf5` 格式自动 30→16 投影。

---

## 七、渲染引擎（hdf5_pipeline/render/engine.py）

```python
render_mp4(
    hdf5_path: str, out_mp4: str,
    show_img=True, show_act=True,
    action_on=None,   # 16 维布尔列表，None = 全开
    left_on=None,     # 7 维关节开关
    right_on=None,
    abort_event=None, # multiprocessing.Event，用于中断
) -> (success: bool, message: str, filename: str)
```

返回三元组 `(是否成功, 状态信息, 文件名)`；用户中断时 success=False 且自动删除半成品文件。

---

## 八、重命名（hdf5_pipeline/rename/engine.py）

| 函数 | 签名 | 说明 |
|---|---|---|
| `collect_hdf5_files` | `(data_dir: str) -> list[Path]` | 递归收集 HDF5，自然排序，排除输出目录 |
| `rename_files` | `(files: list[Path], output_dir: str, if_move=False) -> int` | 按 `episode_XXXXXX` 重命名复制，返回数量 |

---

## 九、Spirit Parquet（hdf5_pipeline/parquet_ui_redo/）

千寻（moz1）spirit 数据专用。入口均以 `raw_dir`（实例目录或含多个实例的目录）为输入。

| 函数 | 签名 | 说明 |
|---|---|---|
| `convert_spirit` | `(raw_dir, out_dir, link_videos=True) -> dict` | 原始 parquet → 标准 LeRobot v2.1 数据集；返回 `{converted, skipped, videos}` 统计 |
| `run_spirit_quality` | `(raw_dir, out_csv, out_json, mask=None, strictness="strict", ...) -> dict` | 异常帧检测，返回摘要 |
| `validate_spirit_dataset` | `(raw_dir) -> (ok, errors)` | 结构完整性 + event_log 一致性校验 |
| `validate_spirit_file` | `(parquet_path) -> (ok, errors)` | 单文件校验 |

---

## 十、GUI 架构

### 10.1 两种页面形态

1. **完整应用** `hdf5_pipeline/label/app.py`：6 个 `st.tabs`（重命名/质量检测/视频渲染/视频打标/数据总览/配置）。
2. **独立模块页** `hdf5_pipeline/ui/module_app.py`：按 `--module` 参数加载单个 `ui/<name>_tab.py` 的 `show_tab_<name>()`。

```python
# ui/module_app.py 的注册机制
MODULES = {
    "rename":  {"title": "文件重命名", "name": "rename"},
    "quality": {"title": "质量检测",   "name": "quality"},
    "render":  {"title": "视频渲染",   "name": "render"},
}
# 动态加载 hdf5_pipeline.ui.<name>_tab 的 show_tab_<name>()
```

### 10.2 新增一个标签页（Tab）

1. 在 `ui/` 新建 `xxx_tab.py`，实现主入口 `def show_tab_xxx() -> None:`。
2. 在 [app.py](hdf5_pipeline/label/app.py) 的 `st.tabs([...])` 追加名称，再 `with tabN: show_tab_xxx()`。
3. 在 [ui/common.py](hdf5_pipeline/ui/common.py) 注册一个 widget key 前缀 `KEY_XXX = "tabxx"`（4 字符、全项目唯一）。

```python
# ui/xxx_tab.py 最小示例
import streamlit as st
from hdf5_pipeline.ui.common import KEY_XXX

def show_tab_xxx() -> None:
    st.subheader("我的新功能")
    text = st.text_input("输入", key=f"{KEY_XXX}_input")
    if st.button("执行", key=f"{KEY_XXX}_go"):
        st.write(f"你输入了：{text}")
```

### 10.3 新增一个独立模块页

1. 实现 `ui/xxx_tab.py` 的 `show_tab_xxx()`（同上）。
2. 在 `module_app.py` 的 `MODULES` 字典加一行 `"xxx": {"title": "页面标题", "name": "xxx"}`。
3. 如需要，在 `cli.py` 的 `UI_MODULES` 列表加上 `"xxx"`。

### 10.4 Widget Key 规范

- 所有 `st.*` widget 必须显式指定 `key=`，且用对应 tab 的 `KEY_*` 前缀，保证全局唯一、可读。
- 规则：`{KEY}_{功能缩写}`，如 `f"{KEY_QUALITY}_src_dir_ti"`。
- 列表型 widget 的 key 内嵌行标识（如 `f"popover_{rec['id']}"`）避免冲突。

### 10.5 状态管理规范

- **读**：`state.get(key, default)`；**写**：`state.set(key, value)`；**删除**：`state.pop(key, default)`。
- 所有默认值在 `init_state()` 中统一用 `setdefault` 初始化。
- **任何对数据库的写操作后必须调用 `bump_db_version()`**——各页靠 `S_DB_VERSION` 版本号比对自动刷新缓存，不手动刷新。
- 一次性提示用 `S_TOAST`：`set(S_TOAST, msg)`，页面顶部统一弹出后清除。
- 空集合一律 `frozenset()`（见上文 `set` 遮蔽）。

### 10.6 配置扩展

- 新增路径：`config.json` 的 `paths` + [core/config.py](hdf5_pipeline/core/config.py) 的 `DEFAULT_PATHS` 同步加一项。
- 新增打标属性：**无需改代码**——在「⚙️ 配置」页添加，或直接写 `config.json` 的 `custom_cols`；`attrs_module` 自动渲染为下拉框。

---

## 十一、开发规范与注意事项

### 11.1 分层与职责

- **UI 层只做渲染与事件回调**，数据逻辑（读写、移动、计算）放业务层函数。
- 文件归档一律复用 `qualify_and_move` / `add_labels`，不要在 UI 里手写 `shutil.move` + SQL。

### 11.2 typing 规范（Pylance / pyright）

项目要求 **pyright 零错误**。检查方式：

```bash
npx -y pyright hdf5_pipeline
```

常见规则：
- 函数参数与返回值写全类型标注，可选参数用 `str | None = None`（不是 `str = None`）。
- `st.session_state.get()` 返回 `Any | None`，传给强类型函数前用 `str(...)` 包裹或提供默认值。
- `h5py` 的 `File[key]` 返回联合类型，用 `np.asarray(...)` 物化，或 `cast(h5py.Group, obj)` / `cast(h5py.Dataset, obj)`。
- 可能未绑定的变量在使用前先初始化（如 `x = 0` / `x = None`）。
- 闭包无法跨作用域收窄：嵌套函数需要的数据用**显式参数**传入。

### 11.3 Streamlit 常见坑

- **`st.button` 只在被点击的那一次 rerun 返回 True**：不要用 `if st.button(...):` 把其他按钮/内容包在内部（后续 rerun 会整体消失）。按钮应只包住它自己的动作。
- `query_records` 返回 `(records, err)` 元组，记得解包。
- `sqlite3.Row` 支持按列名取值，页面里优先用列名。
- 读取视频帧后是 `Optional`，`st.image` 前先判 None。

### 11.4 协作分工模式

- **业务 / 简单逻辑**：可直接由 Claude 实现。
- **核心数据处理逻辑**（数据加载、算法、移动归档策略）：先与 Claude 对齐方法，由你实现，Claude 审查。

### 11.5 测试

```bash
./conda_env/bin/python -m pytest -q
```

新功能建议在 `tests/` 补充对应用例（已有 `test_label` / `test_quality` / `test_render` / `test_rename` / `test_pipeline` / `test_spirit`）。

---

## 附：数据格式速查

```
HDF5 原始 30 维布局（constants.py）:
  [0:7]  left_end_effector_pose   [7:14] right_end_effector_pose   ← 训练不用
  [14:21] left_arm_joints         [21:28] right_arm_joints
  [28] left_gripper               [29] right_gripper

训练 16 维（project_30_to_16）:
  [0:7] left_joints  [7] left_gripper  [8:15] right_joints  [15] right_gripper

质量检测严格度预设（STRICTNESS_PRESETS）:
  loose   min_score=15   medium  min_score=40   strict  min_score=80
```
